<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.teampj_1">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="EasyDoor"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LockPasswordCheckActivity"></activity>
        <activity android:name=".HelpActivity" />
        <activity android:name=".FindAccountIDActivity" />
        <activity android:name=".FindAccountActivity" />
        <activity android:name=".WithdrawlActivity" />
        <activity android:name=".NameChangeActivity" />
        <activity android:name=".PasswordChangeActivity" />
        <activity android:name=".Intro" />
        <activity android:name=".LockPasswordActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".LogoutActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".BluetoothDB" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>